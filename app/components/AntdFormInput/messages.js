/*
 * AntdFormInput Messages
 *
 * This contains all the text for the AntdFormInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AntdFormInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AntdFormInput component!',
  },
});

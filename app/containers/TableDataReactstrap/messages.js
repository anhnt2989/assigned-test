/*
 * TableDataReactstrap Messages
 *
 * This contains all the text for the TableDataReactstrap container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableDataReactstrap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableDataReactstrap container!',
  },
});

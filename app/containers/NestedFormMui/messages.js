/*
 * NestedFormMui Messages
 *
 * This contains all the text for the NestedFormMui container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NestedFormMui';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NestedFormMui container!',
  },
});

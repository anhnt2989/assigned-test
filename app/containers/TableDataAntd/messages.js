/*
 * TableDataAntd Messages
 *
 * This contains all the text for the TableDataAntd container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableDataAntd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableDataAntd container!',
  },
});

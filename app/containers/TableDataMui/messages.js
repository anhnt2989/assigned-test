/*
 * TableDataMui Messages
 *
 * This contains all the text for the TableDataMui container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableDataMui';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableDataMui container!',
  },
});
